# Phase 3: Model Configuration
# This file defines hyperparameters for reproducible training

# Model Architecture
vocab_size = 1000
embedding_dim = 64
num_heads = 4
max_seq_len = 512

# Training Settings
learning_rate = 0.00100000
batch_size = 4
num_epochs = 5
checkpoint_interval = 10

# Paths
model_path = dranzer.pth
checkpoint_dir = checkpoints
